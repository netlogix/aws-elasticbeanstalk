container_commands:
  01_generate_config:
    command: awk '/MemTotal/{print "MaxRequestWorkers", int($2 / 256 / 1024)}' /proc/meminfo > /etc/httpd/conf.d/mpm.conf
  02_apache_restart:
    command: /etc/init.d/httpd restart
